[tool.poetry]
name = "main"
version = "0.1.0"
description = ""
authors = ["raiden <nadirkichou@hotmail.fr>"]
readme = "README.md"
packages = [
    {include = "main", from = "src"},
    {include = "image_loader", from = "src"},
    {include = "image_processors", from = "src"}
]

[tool.poetry.dependencies]
python = "^3.12"
pillow = "^10.4.0"
numpy = "^2.1.1"
pycairo = "^1.27.0"

[tool.poetry.scripts]
main = "main.index:main"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
